---
description: 查询项目规则库和经验库
---

请执行以下操作：

1. 读取 `RULES.md` 文件
2. 如果用户提供了关键词（如 `$ARGUMENTS`），只显示包含该关键词的规则
3. 如果没有关键词，显示全部 7 条规则的摘要（规则编号 + 一句话描述）
4. 如果 `.last_error` 文件存在，也显示最近捕获的错误

输出格式：
```
R1: [规则标题] — [适用/不适用于当前任务]
R2: ...
```
